# ClusterSecretStore for GCP Secret Manager
# Uses Workload Identity - no credentials needed
---
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-manager
spec:
  provider:
    gcpsm:
      projectID: hta-platform-prod
