# =============================================================================
# Kubernetes Service Accounts for Workload Identity
# =============================================================================
# These service accounts are bound to GCP service accounts via Workload Identity
# The annotation links the K8s SA to the GCP SA for automatic token injection

apiVersion: v1
kind: ServiceAccount
metadata:
  name: hta-api
  namespace: hta-platform
  labels:
    app.kubernetes.io/name: hta-api
  annotations:
    # Replace PROJECT_ID with your GCP project ID
    iam.gke.io/gcp-service-account: production-api@PROJECT_ID.iam.gserviceaccount.com
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: web-hta
  namespace: hta-platform
  labels:
    app.kubernetes.io/name: hta-web
  annotations:
    iam.gke.io/gcp-service-account: production-web@PROJECT_ID.iam.gserviceaccount.com
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hta-worker
  namespace: hta-platform
  labels:
    app.kubernetes.io/name: hta-worker
  annotations:
    iam.gke.io/gcp-service-account: production-worker@PROJECT_ID.iam.gserviceaccount.com
