name: GitOps Deploy (Disabled)

# Production deployments are owned by deploy.yml. This non-dispatched workflow
# is kept only as a record of the previous Argo CD deployment approach.
on:
  workflow_call:

jobs:
  disabled:
    name: Direct GKE workflow owns production
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the Deploy to GKE workflow."
